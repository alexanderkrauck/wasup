# Locked Decisions

These are settled. The coding agent follows them without revisiting (CLAUDE.md Class A). Changing anything here requires Alexander's explicit sign-off via OPEN-QUESTIONS.md.

## Stack

| Area | Decision | Rationale (short) |
|---|---|---|
| Language | Python 3.12+ | ecosystem fit (Playwright, LLM SDKs, dateutil), Alexander's stack |
| Package/env | `uv` | fast, lockfile, no poetry ceremony |
| Web framework | FastAPI | standard, pydantic-native |
| DB | PostgreSQL 16 + PostGIS + pgvector, single instance | ARCHITECTURE §11; one box is 100x headroom |
| DB access | psycopg3, **plain SQL** (no ORM) | schema is the spec; ORMs hide the queries that matter |
| Migrations | plain sequential SQL files + tiny runner (or `dbmate`) | no alembic autogen magic |
| Validation | pydantic v2 everywhere (LLM outputs, API IO, recipes) | one validation idiom |
| Job queue | `jobs` table + `SELECT … FOR UPDATE SKIP LOCKED`, one worker process | ARCHITECTURE §H7; no broker |
| Scheduler | cron (system) inserting jobs | boring |
| HTTP fetch | httpx (sync for now - one source/job + 2s domain rate limit leaves no concurrency to exploit; async when that changes) | changelog 2026-07-03 |
| Headless | Playwright, lazy-installed, phase 3 only | ~20-30% of sources need it |
| Recurrence | constrained schema → deterministic compiler → `dateutil.rrule` | HURDLES §H1: LLM never writes RRULE |
| LLM access | one thin client module wrapping a single provider SDK; model names in config | mini-model default, escalation per ARCHITECTURE §model-routing |
| Embeddings | provider API (same client), stored in pgvector | no local models in v1 |
| Tests | pytest; fixtures = recorded HTML snapshots (HURDLES §H3.4); gold set as data file in repo | |
| Deploy | one small VPS (Hetzner-class), systemd services (api, worker), Postgres on same box | no docker required; if containerizing, ONE Dockerfile max |
| Repo layout | single repo, single package `eventindex/`, modules: `db/ fetch/ extract/ resolve/ enrich/ api/ discovery/ jobs/` | monolith |
| Secrets | `.env` file, never committed | |
| Timezone | store UTC, business logic in Europe/Vienna | Austrian holidays table per HURDLES §H1.2 |

## Product decisions (locked)

- Claims are **append-only**; canon is a rebuildable materialized view (HURDLES §H0). The `identity` table is the only non-rebuildable canon state.
- Staleness decay computed **at query time** in the API layer (ARCHITECTURE §7) - never a batch job.
- `null` means **unknown**, never "no" - hard API contract (ARCHITECTURE §7/§9b). Clarified 2026-07-08 with the preference model (Alexander): HARD filters (window, exclusions, categories, price, required_attributes) never match unknowns; SOFT audience preferences score unknowns at a fixed prior (0.45) instead of dropping them - unknown stays visible and honestly ranked, never treated as a match or a "no".
- Negative constraints (exclusions) are set logic before ranking, never embedding similarity (ARCHITECTURE §7).
- Audience-attribute queries combine **caller importance x stored certainty** (Alexander 2026-07-08): P(satisfied) anchored at the coin flip (match 0.5+c/2, contradiction 0.5-c/2, unknown 0.45), importance-weighted mean, exposed as `match_score`. Hard enforcement is opt-in via `required_attributes`. Extension point: the ATTRIBUTES registry in api/search.py (one Enrichment field + one registry row per new attribute).
- Status/availability merge is asymmetric recency-first (ARCHITECTURE §6).
- False-merge is the bad dedup error: precision@merge ≥ 0.98 against gold set, recall may lag (HURDLES §H2.3).
- Crawl politeness: honest UA with contact email, per-domain rate limit ≥ 2s. robots.txt is deliberately ignored (Alexander's explicit sign-off, 2026-07-06: "as long as it's not behind a login wall we must always crawl"). Login walls stay out of scope.
- Probe policy: every candidate scoring ≥ 0.5 registers automatically; doubts become `extraction_hint.probe_concerns` attributes (e.g. membership_required) instead of gating registration (Alexander, 2026-07-06: when in doubt, crawl). Junk decays economically (H4.2).
- Budgets: every LLM/agent call runs inside a budget context; global daily cap enforced in code from day one. Caps: €10/day global LLM (raised from €5 by Alexander 2026-07-06 during discovery ramp-up), per-source defaults per ARCHITECTURE §cost-governance.
- v1 scope fence per CLAUDE.md - re-entry triggers per HURDLES §H7.2.
- Metro boundary: configurable polygon, initially Linz + ~25km (Leonding, Traun, Ansfelden, Enns, Wels-fringe optional) - index generously, filter at serve time (HURDLES §H4.3).

## Deliberately NOT decided (Class B - agent's choice, log below)

Internal module APIs, test organization, exact index DDL, digest formatting, log format, retry/backoff parameters (within budget caps), fingerprint normalization details (within §6 spec).

## Changelog (agent appends one-liners here)

- 2026-07-03: kit created; no code exists yet.
- 2026-07-03: OPEN-QUESTIONS #1-3, #6 answered (OpenRouter, local Docker Postgres for dev, gmail contact, local git); details there.
- 2026-07-03: phase 0 built. Class B picks: src/ layout (uv default), initial models mini=openai/gpt-5-mini mid=anthropic/claude-sonnet-4.5 frontier=anthropic/claude-opus-4.5, retry backoff 60s×5^n (3 attempts), digest to var/digests/ as text file. Dev DB image is pgvector base + PostGIS apt package (postgis/postgis has no arm64).
- 2026-07-03: phase 1 built. Class B picks: fetcher is sync httpx for now (one source/job + 2s domain rate limit = zero concurrency to exploit; async when the scheduler batches); budget ledger writes on own autocommit connection (spend must survive job rollback - money is already gone); linztermine open-data XML gets a dedicated tier-b parser (§3.3 "platform API payload"; a 2nd API format triggers generalizing); 15 top-level category seeds in config; meinbezirk dropped from seeds (nation-mixed feed, region pollution); LLM claims: one claim per date element, 60d horizon.
- 2026-07-07: models swapped to open-weight Chinese models (Alexander: cost): mini=deepseek/deepseek-v4-flash, mid=moonshotai/kimi-k2.6 (onboarding/agentic), frontier=z-ai/glm-5.2. Est. ~4x cheaper/day vs gpt-5-mini+sonnet at same volume. Verification pending credit top-up: first cron cycle exercises all schema-validated paths; gold set + recipe self-validation gate quality. Caches (adjudication, enrichment) keep prior verdicts.
- 2026-07-06 (later): H5 "never invent" relaxed by Alexander: inferred attributes are ALWAYS estimated (LLM world knowledge), confidence encodes guess-ness (~0.2 guess / ~0.35 typical / <=0.8 evidenced). Null only for truly inapplicable. Rationale: the confidence field exists to label guesses; null starves agent-search filters.
- 2026-07-06: demographics-inference scope fence re-entry trigger fired by Alexander ("we want the age estimate... rich llm inferred attributes, each with confidence"). Enrichment per H5: category_priors baseline, LLM adjusts ONLY from explicit textual evidence, confidence capped 0.8, served as labeled estimates. Cached by content hash so canon rebuilds stay free.
- 2026-07-06: phase-4 /v1/search redefined by Alexander: agent-parsed HARD filters first (time window, category/tags, audience, price), set logic in SQL; embeddings at most rank the residual vibe fragment, never select. Note: audience-age filters need the H5 demographics estimator (scope-fence re-entry trigger "product demand" arguably fired; decide at phase-4 start).
- 2026-07-06: Alexander overrode two politeness/gating rules in chat: robots.txt ignored everywhere (login walls remain the only boundary); probe review queue abolished - >=0.5 always registers with probe_concerns attributes. Both edited above in place.
- 2026-07-05: phase 2 built. Class B picks: title_similarity is trigram-only until an embeddings provider is decided (→ OPEN-QUESTIONS #9); §6 blocking loosened to (Vienna day × venue-cell) with the §6 formula deciding within blocks (title-exact blocking would make cross-source title variants unmergeable); resolve job = full rebuild, delete+insert in one tx instead of table-rename blue-green (atomic via MVCC at 2k rows); LLM adjudications + recurrence verifications cached in `adjudication` table on own connections (paid verdicts survive rollbacks, rebuilds are free and idempotent); occurrence ids = uuid5(event_id, starts_at); identity repoints to survivor on merge, mints fresh id on lineage split; explicit-date series threshold >= 3 distinct days; theater runs of 1-2 days stay per-day one_offs.
- 2026-07-07 (one-shot): forward projection shipped (red-team #1): deterministic gap detection (weekly/biweekly, >=3 dates, one holiday-skip tolerated) on explicit series; projects max 4 weeks past last observation and ONLY beyond the sources' demonstrated horizon (extraction_hint.horizon_days) - absence within a feed's reach is evidence, never filled. Projected occurrences carry `occurrence.projected` (migration 010) end-to-end; canon-rebuild semantics make projections self-expiring. Free-text recurrence ("jeden Mittwoch...") is regex-gated into the constrained Recurrence schema, cached by content hash in `text_recurrence`.
- 2026-07-07 (one-shot): adjudicator upgraded (red-team #2): prompt now carries source names, 200-char description snippets, price, URL; mini "different" verdicts at score >= 0.65 get ONE mid-model second opinion (decided_by=llm_mid); adjudicated cross-venue merges auto-alias venues < 300m apart (else review dump). 342 stale mini "different" verdicts invalidated for re-decision. Gold set grown 124->155 rows (31 new labels incl. the cached-miss marquee pairs); precision@merge 1.000 over 23 system merges. Fixed a latent cache poisoner: failed LLM adjudications/verifies (incl. BudgetExceeded) are no longer cached as verdicts; budget errors now bubble so the worker parks the rebuild until the daily reset.
- 2026-07-07 (one-shot): phase-4 remainder shipped: qa_check job (nightly 20-sample + per-report; cancellations flow as claims from an internal 'QA verifier' source per H0, never canon edits; source trust EMA 0.9/0.1), digest QA section, GET /v1/feed.ics (shared filter set with /v1/occurrences), POST /v1/reports -> qa_check, GET /v1/changes (updated_at keyset), api_key table + one dependency function (bootstrap rule: API open until the first active key exists; key accepted as X-API-Key header or ?api_key= for calendar clients), §9b private-intent suppression (residential address + personal-name organizer + no venue -> geo withheld, event stays listed, review dump), 50-query search-leak gate as pytest -m live.
- 2026-07-08 (backfill for 2026-07-07 chat decisions previously only in config.py comments): mid model is moonshotai/kimi-k2.7-code, not k2.6 (k2.7-code is multimodal, covers onboarding + adjudication escalation); frontier tier DELETED (Alexander: "2 tiers might be sufficient" - it had zero call sites; re-add if tier-D crawls ever unfence). Current models: mini=deepseek/deepseek-v4-flash, mid=moonshotai/kimi-k2.7-code.
- 2026-07-08: doc-rot sweep (Alexander requested review): stale spec text annotated in place with pointers here (ARCHITECTURE §2/§6/§9/§11/§12, HURDLES H5/H7.2, BUILD-PLAN phase 1); .env.example dead Google-CSE vars removed; README run instructions synced with the installed cron + API. Historical records (migrations, old changelog lines) left untouched by policy.
- 2026-07-08: code-review sweep (2 independent reviewer agents + mechanical scans), all high-confidence fixes shipped: search NULL-poisoning (exclusions dropped unenriched events) + LLM window strings now validated & Vienna-pinned; failing sources no longer retry forever (worker leaves an error crawl_log despite rollback, monthly-budget jobs park until month rollover, 5 consecutive error crawls -> degraded + self-heal onboard); QA confirmations/cancellations both flow as claims (survive rebuilds) and the confirmation sweep skips internal sources; recipe crawls now pass the same sanity gates as the cascade; recurrence freq=once/irregular never mints series; verify failures are never cached as tentative; recipe caps fixed (calendar expansion un-truncated, detail budget per crawl, _all_seen ignores garbage pages); probe only accepts same-domain listing URLs; onboarding re-raises budget/DB errors instead of paying into poisoned sessions; dormancy reactivates on yield; XML charset content-types detected; JSON-LD relative URLs joined; naive datetimes Vienna-pinned; resource leaks closed; JOB_STALE_RUNNING_S 1h->4h (double-run guard). Deferred semantics-changing findings -> BUILD-PLAN backlog #9.
- 2026-07-08: onboarding budgets made value-aware (Alexander): wall clock 600->1000s; when a session nears a base ring (25 turns / EUR0.60 / 1000s) ONE checkpoint asks the agent, inside the same prefix-cached conversation, for expected_events_per_crawl + expects_success (validated JSON, fail-closed); a deterministic gate scales the allowance at EUR0.03 per expected event, clamped to hard rings EUR2.50 / 60 turns / 1800s. The model provides evidence, the code decides; estimates are trajectory-logged for calibration against actual yield.
- 2026-07-08: agent query surface shipped (Alexander): POST /v1/query takes SearchFilters directly (partial bodies, optional importance weights) so external agents parse queries with THEIR brain - zero index-side LLM cost; /v1/search stays as the budgeted NL convenience. Attribute constraints redefined per Alexander: window/exclusions/categories/price + required_attributes are HARD set logic (null never matches); audience attributes are SOFT preferences ranked by importance x stored certainty, anchored at the coin flip (match 0.5+c/2, contradiction 0.5-c/2, unknown 0.45) - weak guesses beat unknowns beat contradictions, nothing silently dropped; match_score exposed per row. ATTRIBUTES registry in api/search.py is the single extension point for future inferred attributes. Discovery: /llms.txt (llmstxt.org convention, open like /docs), /.well-known/api-catalog (RFC 9727), enriched OpenAPI, drop-in Claude skill at skills/eventindex/SKILL.md. MCP deferred: fastapi-mcp is a 5-line mount when a native-MCP consumer appears; curated 3-tool server, never an endpoint dump.
- 2026-07-08: preference-model integration sweep (Alexander requested): SOFT_ATTRIBUTES constant + validator (unknown required_attributes now 422 instead of silently ignored); typed QueryBody derived programmatically from SearchFilters so POST /v1/query is fully OpenAPI-documented; importance-key naming ("age" covers the age_min/age_max pair) documented in /llms.txt; /v1/occurrences docstring marks it as the raw hard-filter listing; locked null=unknown decision amended with the hard/soft split; ARCHITECTURE §7 (preference formula) + §9 (/v1/query), HURDLES H5 (query side closes the loop), BUILD-PLAN backlog 9c updated.
- 2026-07-08: mid model swapped kimi-k2.7-code -> minimax/minimax-m3 (Alexander asked for cheaper-but-good: $0.30/$1.20 vs $0.74/$3.50 on the onboarding-dominated spend, strong agentic benchmarks, multimodal so the PDF/vision option survives; verified live on OpenRouter). Mini stays deepseek-v4-flash - independently confirmed the consensus cheapest-capable model, nothing below it survives quality scrutiny. Global daily cap 10 -> 15 EUR (Alexander) to drain the onboarding backlog faster. Watch: recipe-success rate in the digest; Kimi is the named fallback.
- 2026-07-08: new inferred primitives solo_friendly + interaction_structure (none|optional|built_in) per Alexander (the shy/meet-people use case = S16, the original itch). Design rule crystallized in llms.txt: store NEUTRAL structural primitives with confidence; spicy queries ("Frauen kennenlernen allein", "where should business X sponsor") are query-time COMPOSITIONS documented as recipes - the index never labels anyone's event as a dating venue. Enrichment SCHEMA_VERSION added to content_key: schema growth re-enriches the corpus (~EUR 0.5).
- 2026-07-08: repo pushed to public GitHub (github.com/alexanderkrauck/eventindex, all rights reserved, no license file - commercialization stays open; go-to-market strategy moved to gitignored private/). Deployed to Alexander's existing netcup VPS (shared box): own pg16+PostGIS+pgvector container on 127.0.0.1:5433, app under dedicated user via systemd, API localhost-only until domain+TLS. Mac cron disabled - the VPS is now the production system.
- 2026-07-09: product named "Wasup" (Alexander), domain wasup.at (registration pending, netcup; A records @ and api -> 152.53.104.103). Reads as the query itself and as a domain hack ("wasup.at/linz"). Code/package/repo stay `eventindex` - brand and code name deliberately decoupled, zero churn. On DNS: Caddy + TLS, API key rotation, /privacy page, domain into llms.txt/skill/README - then the ChatGPT app path is unblocked.
- 2026-07-09: first external consumer (Alexander's laptop Claude) filed a bug report against the live API; all confirmed and fixed same day: /v1/events/{id} 500ed on every enriched event (int4range not JSON-serializable), query/occurrence rows lacked venue_name/address, source-geo fallback leaked as fake "downtown" coordinates for venue-less events (null-contract violation - blocking keeps the fallback, publishing does not), llms.txt served text/markdown which some agent fetchers drop (now text/plain per convention), and llms.txt gained the fine print (null ends_at semantics, price 0-vs-null, match_score is ordinal). Known-deferred confirmed by the consumer too: time-proximate duplicates (backlog 9a).
- 2026-07-09: anonymous read tier opened (Alexander): GET occurrences/events/changes/feed.ics + POST /v1/query are keyless, rate-limited 60/min per IP (XFF-aware behind Caddy, in-process window - the one sanctioned in-memory state outside the worker); a key lifts the limit. /v1/search stays keyed (per-call LLM spend from OUR budget), /v1/reports stays keyed (write). Rationale: the URL becomes the integration - any assistant can use the index with zero setup, at zero LLM cost to us.
- 2026-07-10: completeness = the site's WHOLE published horizon (Alexander: "alle Events die es gibt; 5-Jahre-Limit ok, aber nicht random 3 Monate"): emit_recipe now requires site_horizon_days (how far ahead the agent SAW events); the deep-probe horizon check enforces >= 80% of it, capped at 5 years (MAX_MONTHS_AHEAD 12 -> 60). Verified live: linztermine recipe v3 = single open 24-month window x next_click/80 pages, covers the site's full calendar into 2028. Recurring collapse stays the resolver's job.
- 2026-07-10: limits must scream (Alexander): any hard limit that cuts a PRODUCTIVE crawl short (page/state caps with the next control still active or queued URLs unfetched, budget-parked crawl jobs on yielding sources) is recorded as LIMIT-TRUNCATED in crawl_log and rendered as a loud !!-banner section in the nightly digest ("EVENTS ARE BEING MISSED"). render_states returns (states, more_available); ValidationResult grew a truncated field.
- 2026-07-10: pagination taxonomy grew its 9th type, next_click (Alexander: "fix it so it works retroactively and for all websites"): headless click-through that harvests EVERY page state - covers JSF/PrimeFaces/WebForms paginators that REPLACE the list in place (no recipe in the old set could enumerate linztermine's site search). Companion fixes, all generic: {from}/{to} date templates now expand for every pagination type with per-site date_format (strftime) + chunk_days windows; MAX_PAGES_HARD 60 -> 100; interactive types coerce render=headless; the zero-items validation hint is now evidence-based (checks whether sample_titles are in plain HTTP before suggesting headless); every agent observation reports a link-set delta so ignored ?page= params are detectable in one turn; emit_recipe requires expected_events_on_site and a COVERAGE gate rejects recipes reaching <20% of the agent's own estimate (caught the valid-but-24-events recipe); a min-horizon check deep-probes the deepest reachable page (a chronological listing shows next week on page 1 no matter how deep the recipe goes); worker marks sources degraded after a definitively failed onboard instead of leaving them crawling their homepage. Verified live: linztermine deep re-onboarded in 2 attempts, recipe v2 = date windows x next_click, crawl yields 426 events vs 24 (v1) vs 5 (fallback). 160 tests incl. real-Chromium fixtures.
- 2026-07-09: Wasup branding sweep (Alexander): skill renamed skills/eventindex -> skills/wasup (skill `name: wasup` - the invocation name is a brand surface), README/VPS-doc/coverage-chart labels now "Wasup"/wasup.at; llms.txt/privacy/MCP title already carried the brand. Live URLs stay on wasup.goedly.com - wasup.at has no A record yet (per the 2026-07-09 domain decision the swap happens when DNS lands). Code/package/repo remain `eventindex` per the same decision.
- 2026-07-09: frontend scope fence re-entry trigger fired (Alexander): exactly one dependency-free HTML calendar page at GET / (calendar.html, day-grouped chronological view, cursor pagination, date/category filter) consuming the same keyless /v1/occurrences tier as agents. No framework, no build step, no second page without a new trigger. Rationale: human-checkable visualization of the whole index + the root URL stops 404ing for curious visitors.
- 2026-07-09: MCP server shipped (Alexander; directory listing is the goal): official `mcp` SDK mounted IN the API process at POST /mcp (stateless streamable HTTP + JSON responses - what ChatGPT apps require; exact-path Route because a Starlette Mount 307-redirects POST /mcp -> /mcp/, which MCP clients do not follow). Five read-only tools, all with the title+readOnlyHint/destructiveHint/openWorldHint annotations both directories reject without: search_events (delegates to the /v1/query core, QueryBody stays the single filter schema), get_event, get_calendar_link, plus `search`/`fetch` verbatim (ChatGPT chat-connector contract). No auth (public-read tier); /mcp sits behind the same keyless-60/min-per-IP gate via middleware (mounted/raw routes bypass FastAPI dependencies). Directory prerequisites already live: HTTPS, /privacy. Submission blockers on Alexander's side: OpenAI platform verified-developer account; Anthropic directory needs a Team/Enterprise org (custom connector works for anyone today).
- 2026-07-10: incompleteness red team shipped (Alexander: "some incompleteness red team attacks might be good", full-autonomy go): (1) discovery vocabulary was pure leisure - business/institutional segment unfindable by construction (caught live: startup/pitch query found tech2b, Startup Live, WKO all absent). SEARCH_TERMS 20->46 via a systematic pass over every event-emitting segment, SEARCH_AREAS +Enns/Ansfelden/Ottensheim/Pasching, MAX_SEARCHES_PER_SWEEP 40->80 (~monthly full rotation, +~EUR10/mo inside caps), PLACES_CATEGORIES +7, OSM tags +events_venue/conference_centre/cinema/nightclub/bowling/ice_rink. (2) scripts/recall_redteam.py: per-vertical recall vs independent web ground truth (Exa, ~EUR0.04/vertical), misses auto-become probes (chokepoint still decides), report to var/review. (3) scripts/index_audit.py: precision twin - locality junk (global aggregators pad city listings with online events, e.g. eventbrite.co.uk items in the Linz feed) + out-of-bbox geo + aggregator->canon resolve loss; pure SQL. (4) digest gained a day-curve anomaly section (<50% of weekday median = capped-feed signature). 10 manual probes seeded (startup ecosystem + Volkshaus/Design Center/Kepler Salon/Kunstuni/Bruckneruni). Suppression of measured locality junk = OPEN-QUESTIONS #15 (semantics change, not snuck in).
